# Compiled from lib/*.c by lib/pnp-enter on first use.
/lib/userns-exec
/lib/pnp-mount

# The two overwatch binaries build-images.sh compiles: the real daemon, which
# the harness runs once to record its refusal, and the stand-in it runs
# instead. Host binaries rather than images since D-009 (deploy/README.md).
/bin/
