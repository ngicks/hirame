# Scenario S8 only, applied on top of dropins/. Only the indexer needs it: it
# is the process that runs the River maintenance job (D-008).

[Container]
EnvironmentFile=%h/.config/hirame/eviction-quota.env
